<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>Smokeshow</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>

	<!-- One-shot, only when the user asks. The app never tracks a location. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Smokeshow uses your location once to fetch the smoke forecast where you are. It is never stored on a server and never tied to an account.</string>

	<!-- Silent pushes from the notification backend say "the verdict moved",
	     which is what makes the widget refresh cheap. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
	</array>

	<!-- The smoke-event countdown in the Dynamic Island. -->
	<key>NSSupportsLiveActivities</key>
	<true/>
	<key>NSSupportsLiveActivitiesFrequentUpdates</key>
	<false/>

	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>earth.smokeshow.deeplink</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>smokeshow</string>
			</array>
		</dict>
	</array>

	<!-- Injected at build time; empty in CI, where the stub entitlement
	     provider takes over so the app still runs. -->
	<key>RevenueCatAPIKey</key>
	<string>$(REVENUECAT_API_KEY)</string>

	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UILaunchScreen</key>
	<dict/>
</dict>
</plist>
